void
strings_break_by_whites(char** segments, int max_segs, const char* line_c)
{

	//TODO: impl + bzero + alloca + strlen(+1) + strtok
	//                   use alloca + strlen + strtok ---> convert a line to segments
}
